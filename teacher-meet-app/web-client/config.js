// Edit this after deploying (Render/Netlify/Vercel static-site file editor,
// or just re-push this one file) — no rebuild of renderer.js needed.
// Must match the "signalingUrl" in electron-app/config.json exactly.
window.APP_CONFIG = {
  SIGNALING_URL: "wss://YOUR-SIGNALING-SERVER.onrender.com",
};
